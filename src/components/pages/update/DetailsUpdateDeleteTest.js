import Modal from "../../modals/Modal";
import Backdrop from "../../modals/Backdrop";

function DetailsUpdateDeleteTest (props) {

    return (
        <div>
            <Modal />
            <Backdrop />
        </div>

    )

} // DetailsUpdateDeleteTest
export default DetailsUpdateDeleteTest

// Add Modal & backdrop to the page that you want the button (to open the Modal) to be on. (our case = the all view --- for a Modal that renders the Details or Edit